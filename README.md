# Steps to run
- Install uv (hopefully your package manager should have it) 
- run command: $uv run main.py

# Notes 
- Works well when the peaks are "drifting" slightly upward 
- Does not work when they are "drifting downward"
- Does not handle situations where the blocks aren't tightly coupled
